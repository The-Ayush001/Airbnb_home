  <header class="bg-red-500 text-white p-4 shadow-md">
    <nav class="container mx-auto flex justify-between items-center"></nav>
      <ul class="flex space-x-4">
        <li>
          <a href="/" class="<%= currentPage === 'index' ? 'bg-red-600': 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Go to Home</a>
        </li>
        <li>
          <a href="/homes" class="<%= currentPage === 'Home' ? 'bg-red-600': 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Home-List</a>
        </li>
        <li>
          <a href="/bookings" class="<%= currentPage === 'Bookings' ? 'bg-red-600' : 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Booking</a>
        </li>
        <li>
        <li>
          <a href="/favourite" class="<%= currentPage === 'Favourite' ? 'bg-red-600' : 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Favourite</a>
        </li>
        <li>
          <a href="/host/host-home-list" class="<%= currentPage === 'host-Home' ? 'bg-red-600' : 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Host Home</a>
        </li>
        <li>
          <a href="/host/add-home" class="<%= currentPage === 'Add-Home' ? 'bg-red-600' : 'hover:bg-red-600' %> text-white py-2 px-4 rounded transition duration-300">Add Home</a>
        </li>
      </ul>
    </nav>
  </header>